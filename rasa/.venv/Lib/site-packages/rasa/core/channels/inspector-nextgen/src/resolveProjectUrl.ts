/** Resolves projectUrl from URL search params with dev/prod fallbacks. */
export const resolveStandaloneProjectUrl = (searchParams: URLSearchParams): string =>
  searchParams.get("projectUrl") ||
  (import.meta.env.DEV ? "http://localhost:5005" : globalThis.location.origin);
