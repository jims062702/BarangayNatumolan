import { fireEvent, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { renderWithProviders } from "../../../tests/utils";
import { ConversationEventType, type ConversationEvent } from "../../../types";
import { TtsFinishedEvent } from "./TtsFinishedEvent";

const baseEvent: ConversationEvent = {
  __typename: "ConversationEvent",
  id: "1",
  conversationEventType: ConversationEventType.TtsFinished,
  name: "tts_finished",
  actionText: "",
  flowId: "",
  slotValue: null,
  stepId: "",
  timestamp: new Date().toISOString(),
  originalTimestamp: 0,
  metadata: {
    parseData: {},
  },
};

describe("TtsFinishedEvent", () => {
  it("renders 'TTS finished' label", () => {
    renderWithProviders(<TtsFinishedEvent event={baseEvent} isSelected={false} />);

    expect(screen.getByText("TTS finished")).toBeInTheDocument();
  });

  it("renders as a button", () => {
    renderWithProviders(<TtsFinishedEvent event={baseEvent} isSelected={false} />);

    expect(screen.getByRole("button")).toBeInTheDocument();
  });

  describe("hover actions", () => {
    const actions = [{ icon: {} as never, label: "Copy", action: vi.fn() }];

    it("does not show action button when not hovered", () => {
      renderWithProviders(
        <TtsFinishedEvent
          event={baseEvent}
          isSelected={false}
          conversationEventActions={actions}
        />,
      );

      expect(screen.queryByText("Copy")).not.toBeInTheDocument();
    });

    it("shows action button on hover", () => {
      renderWithProviders(
        <TtsFinishedEvent
          event={baseEvent}
          isSelected={false}
          conversationEventActions={actions}
        />,
      );

      fireEvent.mouseEnter(screen.getByRole("button"));
      expect(screen.getByText("Copy")).toBeInTheDocument();
    });

    it("hides action button after mouse leaves", () => {
      renderWithProviders(
        <TtsFinishedEvent
          event={baseEvent}
          isSelected={false}
          conversationEventActions={actions}
        />,
      );

      const container = screen.getByRole("button");
      fireEvent.mouseEnter(container);
      fireEvent.mouseLeave(container);
      expect(screen.queryByText("Copy")).not.toBeInTheDocument();
    });
  });
});
