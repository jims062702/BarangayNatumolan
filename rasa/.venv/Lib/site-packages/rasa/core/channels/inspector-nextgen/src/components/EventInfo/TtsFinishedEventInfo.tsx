import { Accordion } from "@chakra-ui/react";
import type { ConversationEvent } from "../../types";
import { CommonEventInfo, DetailView, EventAccordionItem } from "./blocks";
import { BotLatencyAccordionItem } from "./LatencyDetails";

interface TtsFinishedEventInfoProps {
  event: ConversationEvent;
  onClose: () => void;
}

export const TtsFinishedEventInfo = ({ event, onClose }: TtsFinishedEventInfoProps) => {
  return (
    <DetailView title="TTS finished" onClose={onClose}>
      <Accordion.Root collapsible multiple>
        <BotLatencyAccordionItem voiceLatency={event.metadata?.voice_latency} />
        <EventAccordionItem title="Event details" value="event-details">
          <CommonEventInfo event={event} />
        </EventAccordionItem>
      </Accordion.Root>
    </DetailView>
  );
};
