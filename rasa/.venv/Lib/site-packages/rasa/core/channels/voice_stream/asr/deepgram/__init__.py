"""Deepgram ASR engine package.

Public surface
--------------
- :class:`DeepgramASR` — the engine class registered under the ``"deepgram"`` key.
- :class:`DeepgramASRConfig` — discriminated-union type alias (v1 or v2 variant).
- :class:`DeepgramV1ASRConfig` — config for ``/v1/listen`` (Nova models).
- :class:`DeepgramV2ASRConfig` — config for ``/v2/listen`` (Flux models).

Internal modules
----------------
- :mod:`.constants` — shared constants (``V2_MODEL_PREFIXES``).
- :mod:`.config` — Pydantic models and discriminator logic.
- :mod:`._flux` — pure v2 event-parsing helpers.
- :mod:`.engine` — ``DeepgramASR`` implementation.
"""

from rasa.core.channels.voice_stream.asr.deepgram.config import (
    DeepgramASRConfig,
    DeepgramV1ASRConfig,
    DeepgramV2ASRConfig,
)
from rasa.core.channels.voice_stream.asr.deepgram.engine import DeepgramASR

__all__ = [
    "DeepgramASR",
    "DeepgramASRConfig",
    "DeepgramV1ASRConfig",
    "DeepgramV2ASRConfig",
]
