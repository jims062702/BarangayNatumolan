from __future__ import annotations

import asyncio
import base64
import json
import time
import uuid
import xml.etree.ElementTree as ET
from typing import (
    Any,
    AsyncIterator,
    Dict,
    Optional,
    Text,
    Union,
)

import structlog
from sanic import (  # type: ignore[attr-defined]
    Blueprint,
    HTTPResponse,
    Request,
    Websocket,
    response,
)

from rasa.core.channels.channel import RuntimeAgent
from rasa.core.channels.voice_ready.utils import (
    CallParameters,
    validate_username_password_credentials,
)
from rasa.core.channels.voice_stream.audio_bytes import L16_24KHZ, RasaAudioBytes
from rasa.core.channels.voice_stream.call_state import (
    BotIsSpeaking,
    BotStoppedSpeaking,
    Marker,
    MarkerType,
    call_state,
)
from rasa.core.channels.voice_stream.tts.tts_engine import TTSEngine
from rasa.core.channels.voice_stream.util import repack_voice_credentials
from rasa.core.channels.voice_stream.voice_channel import (
    ContinueConversationAction,
    DTMFInputAction,
    EndConversationAction,
    MarkerInput,
    MarkerMessageOutput,
    NewAudioAction,
    VoiceChannelAction,
    VoiceInputChannel,
    VoiceOutputChannel,
)

logger = structlog.get_logger(__name__)

SIGNALWIRE_WEBSOCKET_PATH = "webhooks/signalwire/websocket"
SIGNALWIRE_STREAM_CODEC = "L16@24000h"

# Maximum amount of audio (in seconds) we let queue ahead of real-time
# playback at SignalWire's end before we throttle ourselves. SignalWire
# queues ``media`` events for sequential playback, and many TTS engines
# (Deepgram, Cartesia, ...) synthesize 2-5x faster than real time, so for
# long bot replies an unpaced sender fills the queue faster than SignalWire
# can play it; the queue then drops/fast-forwards events and the listener
# hears the end of the message rushed or skipped. Keeping ~1s of look-ahead
# absorbs network jitter without overflowing SignalWire's queue.
SIGNALWIRE_MAX_LOOKAHEAD_SECONDS = 1.0

CALL_SID_REQUEST_KEY = "CallSid"
FROM_NUMBER_REQUEST_KEY = "From"
TO_NUMBER_REQUEST_KEY = "To"
DIRECTION_REQUEST_KEY = "Direction"


def _request_value(request: Request, key: Text) -> Text:
    """Return a SignalWire webhook value from form, query, or JSON data."""
    value = request.form.get(key) if request.form else None
    if value is None and request.args:
        value = request.args.get(key)
    content_type = request.headers.get("content-type", "")
    if value is None and "json" in content_type and isinstance(request.json, dict):
        value = request.json.get(key)
    return str(value) if value is not None else ""


def _websocket_stream_url(server_url: str) -> str:
    """Return the websocket stream URL."""
    # depending on the config value, the url might contain http as a
    # protocol or not - we'll make sure both work
    normalized_server_url = server_url.strip().rstrip("/")
    if not normalized_server_url:
        raise ValueError("SignalWire server_url must be configured")
    if normalized_server_url.startswith("http"):
        base_url = normalized_server_url.replace("http", "ws")
        if base_url.startswith("ws://"):
            logger.warning(
                "signalwire.websocket_stream_url",
                message=(
                    "Forcing wss protocol, since ws is not supported by SignalWire"
                ),
            )
            base_url = f"wss://{base_url[len('ws://'):]}"
    else:
        base_url = f"wss://{normalized_server_url}"
    return f"{base_url}/{SIGNALWIRE_WEBSOCKET_PATH}"


def _signalwire_call_parameters(request: Request) -> Dict[Text, Text]:
    """Map SignalWire webhook fields into stream custom parameters."""
    return {
        "call_id": _request_value(request, CALL_SID_REQUEST_KEY),
        "user_phone": _request_value(request, FROM_NUMBER_REQUEST_KEY),
        "bot_phone": _request_value(request, TO_NUMBER_REQUEST_KEY),
        "direction": _request_value(request, DIRECTION_REQUEST_KEY),
    }


def _signalwire_status_callback_data(request: Request) -> Dict[Text, Any]:
    """Return SignalWire status callback fields from form, query, or JSON body.

    SignalWire sends the StatusCallback as a regular HTTP request whose method
    can be configured to either GET or POST. POST requests carry the fields as
    form-encoded data while GET requests carry them as URL query parameters.
    """
    data: Dict[Text, Any] = {}
    if request.args:
        data.update({key: request.args.get(key) for key in request.args})
    if request.form:
        data.update({key: request.form.get(key) for key in request.form})
    content_type = request.headers.get("content-type", "")
    if "json" in content_type and isinstance(request.json, dict):
        data.update(request.json)
    return data


def _signalwire_stream_response(
    websocket_url: Text, call_parameters: Dict[Text, Text]
) -> Text:
    """Create SignalWire cXML that connects the call to Rasa audio streaming."""
    root = ET.Element("Response")
    connect = ET.SubElement(root, "Connect")
    stream = ET.SubElement(
        connect,
        "Stream",
        {"url": websocket_url, "codec": SIGNALWIRE_STREAM_CODEC},
    )
    for name, value in call_parameters.items():
        ET.SubElement(stream, "Parameter", {"name": name, "value": value})

    xml_body = ET.tostring(root, encoding="unicode")
    return f'<?xml version="1.0" encoding="UTF-8"?>\n{xml_body}'


def map_call_params(data: Dict[Text, Any]) -> CallParameters:
    """Map the SignalWire stream parameters to the CallParameters dataclass."""
    stream_sid = data["start"]["streamSid"]
    parameters = data["start"]["customParameters"]
    return CallParameters(
        call_id=parameters.get("call_id", ""),
        user_phone=parameters.get("user_phone", ""),
        bot_phone=parameters.get("bot_phone", ""),
        direction=parameters.get("direction"),
        stream_id=stream_sid,
    )


class SignalWireOutputChannel(VoiceOutputChannel):
    def __init__(
        self,
        *args: Any,
        max_lookahead_seconds: float = SIGNALWIRE_MAX_LOOKAHEAD_SECONDS,
        **kwargs: Any,
    ) -> None:
        super().__init__(*args, **kwargs)
        if max_lookahead_seconds <= 0:
            raise ValueError(
                "max_lookahead_seconds must be positive; got "
                f"{max_lookahead_seconds!r}."
            )
        self.max_lookahead_seconds = max_lookahead_seconds
        # Wall-clock anchor for the current real-time pacing window and the
        # amount of audio (in seconds) we have sent since that anchor. The
        # anchor is re-set on the first send of each contiguous audio burst
        # so silences/gaps between bot turns don't artificially throttle the
        # next message.
        self._pacing_anchor_time: Optional[float] = None
        self._pacing_audio_seconds_sent: float = 0.0

    @classmethod
    def name(cls) -> str:
        return "signalwire"

    def rasa_audio_bytes_to_channel_bytes(
        self, rasa_audio_bytes: RasaAudioBytes
    ) -> bytes:
        return base64.b64encode(rasa_audio_bytes.data)

    def create_marker_message(self, marker_input: MarkerInput) -> MarkerMessageOutput:
        message_id = uuid.uuid4().hex
        mark_message = json.dumps(
            {
                "event": "mark",
                "streamSid": marker_input.recipient_id,
                "mark": {"name": message_id},
            }
        )

        if marker_input.marker_type in {MarkerType.START, MarkerType.END}:
            call_state.set_marker(
                Marker(
                    marker_id=message_id,
                    marker_type=marker_input.marker_type,
                    step_type=marker_input.step_type,
                )
            )

        return MarkerMessageOutput(message_id=message_id, message=mark_message)

    def channel_bytes_to_message(self, recipient_id: str, channel_bytes: bytes) -> str:
        return json.dumps(
            {
                "event": "media",
                "streamSid": recipient_id,
                "media": {
                    "payload": channel_bytes.decode("utf-8"),
                },
            }
        )

    async def _stream_audio_to_channel(
        self,
        recipient_id: str,
        audio_stream: AsyncIterator[RasaAudioBytes],
        track_latency: bool = True,
    ) -> Optional[RasaAudioBytes]:
        """Stream audio after clearing pacing state left over from a prior barge-in."""
        if self.tts_engine.stop_streaming_output_audio_chunks:
            self._reset_pacing()
        return await super()._stream_audio_to_channel(
            recipient_id, audio_stream, track_latency
        )

    async def send_audio_bytes(
        self, recipient_id: str, audio_bytes: RasaAudioBytes
    ) -> None:
        """Send audio while pacing ourselves to real-time at SignalWire.

        Streaming TTS engines emit audio faster than real-time playback;
        without throttling, long bot replies overflow SignalWire's media
        queue and the end of the message is dropped or fast-forwarded. We
        cap how far ahead of real-time we may queue audio and ``asyncio``
        sleep until we drop back below the threshold, leaving control of
        the actual data rate at SignalWire.
        """
        if not await self._wait_for_pacing(audio_bytes):
            return
        await super().send_audio_bytes(recipient_id, audio_bytes)

    async def _wait_for_pacing(self, audio_bytes: RasaAudioBytes) -> bool:
        """Block until queueing ``audio_bytes`` keeps us within the budget.

        Returns ``True`` when the caller should proceed with the send and
        ``False`` when the chunk must be dropped because the bot stream was
        interrupted (barge-in) or the websocket has failed while we were
        sleeping. Without this check, a chunk parked in ``asyncio.sleep``
        when ``interrupt_playback`` fires a ``clear`` event to SignalWire
        would land *after* the clear and play out of turn.
        """
        if self._should_skip_send():
            self._reset_pacing()
            return False

        bytes_per_sec = audio_bytes.format.bytes_per_second
        if bytes_per_sec <= 0 or len(audio_bytes.data) == 0:
            return True

        chunk_seconds = len(audio_bytes.data) / bytes_per_sec
        now = time.monotonic()

        # (Re-)anchor when we have no anchor yet, or when wall-clock playback
        # has caught up with everything we have sent so far (SignalWire has
        # played everything we have sent). The latter happens between bot
        # turns and avoids carrying an outdated deficit into a fresh utterance.
        if (
            self._pacing_anchor_time is None
            or now - self._pacing_anchor_time >= self._pacing_audio_seconds_sent
        ):
            self._pacing_anchor_time = now
            self._pacing_audio_seconds_sent = 0.0

        elapsed_time = now - self._pacing_anchor_time

        # How far ahead of real-time we would be after sending this chunk.
        ahead_after_send = (
            self._pacing_audio_seconds_sent + chunk_seconds - elapsed_time
        )
        if ahead_after_send > self.max_lookahead_seconds:
            await asyncio.sleep(ahead_after_send - self.max_lookahead_seconds)
            # While we were sleeping, the user may have started speaking
            # (triggering a SignalWire ``clear``) or the websocket may have
            # failed. In either case the queued chunk is no longer safe to
            # deliver - sending it now would play after the clear or be a
            # write to a dead websocket.
            if self._should_skip_send():
                self._reset_pacing()
                return False

        self._pacing_audio_seconds_sent += chunk_seconds
        return True

    def _should_skip_send(self) -> bool:
        """Check whether a pending audio send should be dropped.

        Returns ``True`` when the bot stream has been interrupted (barge-in
        flips ``stop_streaming_output_audio_chunks`` on the TTS engine, the
        same flag the base streaming loop honours) or when the websocket
        has been marked as failed by a prior send. Both conditions mean
        any chunk currently parked in the pacing sleep must not be sent.
        """
        return (
            call_state.connection_failed
            or self.tts_engine.stop_streaming_output_audio_chunks
        )

    def _reset_pacing(self) -> None:
        self._pacing_anchor_time = None
        self._pacing_audio_seconds_sent = 0.0


class SignalWireInputChannel(VoiceInputChannel):
    def __init__(
        self,
        server_url: str,
        asr_config: Dict,
        tts_config: Dict,
        interruptions: Optional[Dict[str, int]] = None,
        silence_timeout: Optional[Union[float, int]] = None,
        username: Optional[Text] = None,
        password: Optional[Text] = None,
    ):
        super().__init__(
            server_url=server_url,
            asr_config=asr_config,
            tts_config=tts_config,
            interruptions=interruptions,
        )
        self.username = username
        self.password = password
        self.silence_timeout = silence_timeout
        self.audio_format = L16_24KHZ

    @classmethod
    def from_credentials(
        cls,
        credentials: Optional[Dict[str, Any]],
    ) -> VoiceInputChannel:
        cls.validate_credentials(credentials)
        new_creds = repack_voice_credentials(credentials)
        return cls(**new_creds)

    @classmethod
    def validate_credentials(
        cls,
        credentials: Optional[Dict[str, Any]],
    ) -> None:
        cls.validate_basic_credentials(credentials)
        username = credentials.get("username") if credentials else None
        password = credentials.get("password") if credentials else None
        validate_username_password_credentials(username, password, "SignalWire")

    @classmethod
    def name(cls) -> str:
        return "signalwire"

    def get_sender_id(self, call_parameters: CallParameters) -> str:
        """Get the sender ID for the channel.

        SignalWire uses the Stream ID as sender ID because it is required
        in OutputChannel.send_text_message to address messages back to the stream.
        """
        return call_parameters.stream_id  # type: ignore[return-value]

    def channel_bytes_to_rasa_audio_bytes(self, input_bytes: bytes) -> RasaAudioBytes:
        return RasaAudioBytes(base64.b64decode(input_bytes), format=self.audio_format)

    async def collect_call_parameters(
        self,
        channel_websocket: Websocket,
        request: Optional[Any] = None,
    ) -> Optional[CallParameters]:
        logger.info("signalwire.collect_call_parameters")
        async for message in channel_websocket:
            data = json.loads(message)
            if data["event"] == "connected":
                logger.info("signalwire.call_connected", data=data)
            elif data["event"] == "start":
                logger.info("signalwire.call_started", data=data)
                return map_call_params(data)
            else:
                logger.warning(
                    "signalwire.collect_call_parameters.unexpected_event",
                    payload=data,
                )
        return None

    async def handle_mark_event(
        self, data: Dict[str, Any]
    ) -> Optional[EndConversationAction]:
        marker = call_state.get_marker(data["mark"]["name"])
        if marker:
            call_state.remove_marker(marker.marker_id)

            if marker.step_type:
                if marker.marker_type == MarkerType.START:
                    call_state.current_bot_utterance_type = marker.step_type
                    await call_state.enqueue_event(BotIsSpeaking())

                if marker.marker_type == MarkerType.END:
                    call_state.current_bot_utterance_type = None
                    await call_state.enqueue_event(BotStoppedSpeaking())
                    if call_state.should_hangup:
                        logger.debug("signalwire.hangup", marker=marker)
                        return EndConversationAction()
        return None

    async def map_input_message(
        self,
        message: Any,
        ws: Websocket,
    ) -> VoiceChannelAction:
        data = json.loads(message)
        if data["event"] == "media":
            audio_bytes = self.channel_bytes_to_rasa_audio_bytes(
                data["media"]["payload"]
            )
            return NewAudioAction(audio_bytes)
        elif data["event"] == "stop":
            return EndConversationAction()
        elif data["event"] == "dtmf":
            return DTMFInputAction(digit=data["dtmf"]["digit"])
        elif data["event"] == "mark":
            mark_event = await self.handle_mark_event(data)
            if mark_event is not None:
                return mark_event
        else:
            logger.warning("signalwire.unknown_event_received", sw_event=data["event"])
        return ContinueConversationAction()

    def create_output_channel(
        self, voice_websocket: Websocket, tts_engine: TTSEngine
    ) -> VoiceOutputChannel:
        return SignalWireOutputChannel(
            voice_websocket=voice_websocket,
            tts_engine=tts_engine,
            tts_cache=self.tts_cache,
            audio_format=self.audio_format,
        )

    async def interrupt_playback(
        self, ws: Websocket, call_parameters: CallParameters
    ) -> None:
        """Interrupt the current playback of audio."""
        logger.debug("signalwire.interrupt_playback")
        await ws.send(
            json.dumps(
                {
                    "event": "clear",
                    "streamSid": call_parameters.stream_id,
                }
            )
        )

    def conversation_blueprint(self, agent: RuntimeAgent) -> Blueprint:
        """Defines a Sanic blueprint for the SignalWire voice input channel."""
        blueprint = Blueprint("signalwire", __name__)

        @blueprint.route("/", methods=["GET"])
        async def health(_: Request) -> HTTPResponse:
            return response.json({"status": "ok"})

        @blueprint.route("/webhook", methods=["GET", "POST"])
        async def webhook(request: Request) -> HTTPResponse:
            call_parameters = _signalwire_call_parameters(request)
            websocket_url = _websocket_stream_url(self.server_url)
            logger.info(
                "signalwire.script_received",
                call_id=call_parameters["call_id"],
                from_=call_parameters["user_phone"],
                to=call_parameters["bot_phone"],
                websocket_url=websocket_url,
            )
            return response.text(
                _signalwire_stream_response(websocket_url, call_parameters),
                content_type="application/xml",
            )

        @blueprint.route("/call_status", methods=["GET", "POST"])
        async def call_status(request: Request) -> HTTPResponse:
            logger.debug(
                "signalwire.call_status",
                request_data=_signalwire_status_callback_data(request),
            )
            return response.json({"status": "ok"})

        @blueprint.websocket("/websocket")  # type: ignore
        async def handle_message(request: Request, ws: Websocket) -> None:
            logger.info("signalwire.handle_message")
            try:
                await self.run_audio_streaming(agent, ws)
            except Exception as e:
                logger.error("signalwire.websocket_error", error=str(e))

        return blueprint
