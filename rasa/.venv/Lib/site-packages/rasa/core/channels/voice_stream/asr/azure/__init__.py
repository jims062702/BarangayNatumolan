"""Azure ASR engine package.

Public surface
--------------
- :class:`AzureASR` — the engine class registered under the ``"azure"`` key.
- :class:`AzureASRConfig` — configuration model for the Azure ASR engine.

Internal modules
----------------
- :mod:`.constants` — shared constants (``DEFAULT_LANGUAGE``).
- :mod:`.config` — Pydantic configuration model.
- :mod:`.engine` — ``AzureASR`` implementation.
"""

from rasa.core.channels.voice_stream.asr.azure.config import AzureASRConfig
from rasa.core.channels.voice_stream.asr.azure.engine import AzureASR

__all__ = [
    "AzureASR",
    "AzureASRConfig",
]
